interface CountryType {
  name: string;
  flag?: string;
  shortName?: string;
  capital?: string;
  continent?: string;
  currency?: string;
  population?: number;
  region?: string;
  borders?: string[];
}

export default CountryType;
